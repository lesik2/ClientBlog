import { Sen, Inter } from 'next/font/google';

export const sen = Sen({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
  variable: '--font-sen',
});
export const inter = Inter({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
  variable: '--font-inter',
});
